apiVersion: 1

providers:
  - name: "LLM Gateway"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      # JSON dashboards live here, mounted from ./config/grafana/dashboards
      path: /etc/grafana/dashboards
